<?php

namespace {{ namespace }}\Controller;

{% block use_statements %}
use UCI\Boson\BackendBundle\Controller\BackendController;
{% if 'annotation' == format.routing -%}
use Sensio\Bundle\FrameworkExtraBundle\Configuration\Route;
{% endif %}
{% endblock use_statements %}

{% block class_definition %}
class {{ controller }}Controller extends BackendController
{% endblock class_definition %}
{
{% block class_body %}
    {% if 'annotation' == format.routing -%}
    /**
     * @Route(path="/{{ bundle_base_name }}/admin/scripts/config.{{ app_name }}.js", name="{{ bundle_base_name }}_app_config")
     */
    {% endif -%}
    public function getAppAction()
    {
        return $this->jsResponse('{{ bundle }}:Scripts:config.js.twig');
    }

{% endblock class_body %}
}
